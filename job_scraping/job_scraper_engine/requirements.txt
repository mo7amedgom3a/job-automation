# Core scraping engine
scrapling[all]   # installs Playwright + fingerprint spoofing deps

# Optional but strongly recommended for I/O-heavy crawls
uvloop; sys_platform != "win32"   # faster async event loop (Linux / macOS)

playwright
chromium
# Nothing else is needed — scrapling bundles its own HTTP client,
# browser automation (Playwright), and fast JSON serialisation.
